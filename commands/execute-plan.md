Execute the plan. $ARGUMENTS

Follow the refactor skill protocol:
- One step at a time
- Run verification checklist after each step
- If no tests cover affected code, stop and ask
- Update PLAN status after each step
- Commit after each step

Start with the next PENDING step.
