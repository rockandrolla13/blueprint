<!-- GENERATED FILE — DO NOT EDIT BY HAND -->
<!-- Regenerate: python3 tools/registry_check.py --write   Verify: python3 tools/registry_check.py --check -->

# Skill Index

Generated from `*/SKILL.md` frontmatter and `## Contract (BCS-1.0)` blocks by `tools/registry_check.py`. Every cell is derived from file content; `[not declared]` means the skill does not state it. Do not edit this file — edit the skill, then run `python3 tools/registry_check.py --write`.

**This is not a loader.** Claude Code builds its available-skills list from a plugin/registry cache, not by reading this file. This index is repo documentation and a drift detector for humans and agents reading the repo. `--check` exits non-zero when it goes stale, so it is CI-able.

14 skills, 16 commands on disk.

## When to use what

| Skill | Mode | Purpose | Consumes | Feeds | Do NOT use when |
|---|---|---|---|---|---|
| **architect** | READ-ONLY | Decompose a domain problem into the right software abstractions — bounded contexts, modules, classes, functio… | `ideate`, `review-architecture` | `design`, `plan-tracker` | Do NOT trigger if the user already knows their components and just needs them wired together — that's the design skill. |
| **code-review** | READ-ONLY | Reviewing Python code FILE BY FILE for correctness, conciseness, and adherence to project conventions. | — | `refactoring-plan`, `review-architecture` | Do NOT trigger for system-level structure, boundary quality, or dependency direction — that is review-architecture. |
| **compat-audit** | READ-ONLY | Audit whether two or more instruction sources — SKILL.md files, pressure packs, CLAUDE.md, AGENTS.md, .cursor… | — | `design`, `plan-tracker`, `refactoring-plan` | Do NOT trigger for reviewing Python source files (use code-review), system-level code structure (use review-architecture), module interface depth (us… |
| **design** | READ-ONLY until approved at gate | Structured architecture design phase that produces a dependency graph, data flow, interfaces, and a review ch… | `architect` | `scaffold` | Do NOT trigger on the word "architect" or on "what should the components be", "how should I decompose this", "where do I draw the boundaries" — that… |
| **ideate** | READ-ONLY | Explore the solution space and stress-test design thinking before committing to an approach. | — | `architect` | Do NOT trigger for "build this", "implement", or "write code" — those belong to the design or scaffold skills. |
| **navigator** | READ-ONLY, INTERACTIVE. A multi-turn Q&A session, not a sin… | Interactive Q&A mode for exploring blueprint and any codebase. | `ideate` | `architect`, `design`, `ideate`, `plan-tracker`, `refactoring-plan`, `review-architecture`, `scaffold`, `spec-interview` | Do NOT trigger for action requests like "refactor X" or "add Y" — those go to action skills. |
| **orch** | READ-ONLY. Wholly. This skill authors a plan file and runs… | Decide whether a build should be delegated to orch's conductor — parallel agents in isolated git worktrees, t… | `architect`, `design`, `ideate`, `plan-tracker`, `scaffold` | `plan-tracker` | Do NOT trigger before a file-level decomposition exists; deciding what the pieces are belongs to architect and design, and orch runs after them, neve… |
| **plan-tracker** | CROSS-CUTTING (active in every workflow) | Create, update, and verify tracked execution plans for multi-step work. | `design`, `refactoring-plan`, `scaffold` | `design`, `refactor`, `scaffold` | Do NOT trigger for single-step tasks, quick questions, or exploratory ideation that hasn't committed to an approach. |
| **refactor** | WRITES CODE (after refactoring-plan gate approval) | Restructure existing code to follow Clean Code, DRY, and extensibility principles without changing external b… | `refactoring-plan` | — | Do NOT trigger for new designs from scratch (use the design skill) or for bug fixes that don't involve structural changes. |
| **refactoring-plan** | READ-ONLY | Synthesise findings from code reviews and architecture reviews into a prioritised, dependency- ordered refact… | `code-review`, `compat-audit`, `review-architecture`, `review-depth` | `plan-tracker`, `refactor` | Do NOT trigger for executing refactoring (use refactor), conducting reviews (use code-review or review-architecture), or designing new systems (use a… |
| **review-architecture** | READ-ONLY | Evaluate the architecture and structural decomposition of an existing codebase against principled design heur… | — | `architect`, `code-review`, `refactoring-plan` | Do NOT trigger for file-level code review (use code-review), refactoring execution (use refactor), or new system design (use architect + design). |
| **review-depth** | READ-ONLY | Review Python code for deep module design and progressive disclosure. | — | `architect`, `code-review`, `ideate`, `refactoring-plan`, `review-architecture` | Do NOT trigger for general code review (use code-review), architecture scoring (use review-architecture), or refactoring execution (use refactor). |
| **scaffold** | WRITES CODE (after design gate approval) | Generate boilerplate code from reusable patterns and templates for common project structures. | `design` | `plan-tracker` | Do NOT trigger for refactoring existing code (use the refactor skill) or for open-ended design (use the design skill). |
| **spec-interview** | READ-ONLY until approved at Phase 4 gate; then WRITES ONE M… | Interrogate an EXISTING markdown design doc, spec, RFC, or plan on disk until its ambiguities are resolved, t… | `ideate` | `architect`, `design`, `ideate`, `scaffold` | Do NOT trigger for ideate — ideate takes a freeform problem with no input file and outputs a DECISION about which approach to take; spec-interview as… |

`Consumes` / `Feeds` list only sibling skills named in that skill's own `### Consumes` / `### Downstream Consumers` contract sections. `—` means none named.

## Full negative triggers

Verbatim from each `description:`. This is what stops two skills firing on the same prompt.

**architect**
- Do NOT trigger if the user already knows their components and just needs them wired together — that's the design skill.
- Do NOT trigger for refactoring existing code or generating boilerplate.

**code-review**
- Do NOT trigger for system-level structure, boundary quality, or dependency direction — that is review-architecture.
- Do NOT trigger for module interface depth, cognitive load, or information hiding — that is review-depth.
- Do NOT trigger for conflicts between instruction files, skills, or rule sets — that is compat-audit.
- Do NOT trigger to CHANGE code — this skill is read-only; execution is refactor.
- Do NOT trigger for paper/manuscript review or general writing feedback.

**compat-audit**
- Do NOT trigger for reviewing Python source files (use code-review), system-level code structure (use review-architecture), module interface depth (use review-depth), or exploring solution approaches (use ideate).

**design**
- Do NOT trigger on the word "architect" or on "what should the components be", "how should I decompose this", "where do I draw the boundaries" — that is the architect skill, which runs BEFORE this one and whose Handoff this skill consumes.
- Do NOT trigger on a bare transition from ideation ("ok let's go with approach X, design it") — that routes to architect first; design cannot accept an ideate Handoff.
- Do NOT trigger for single-function tasks, bug fixes, or refactoring (those have their own skills).
- Do NOT trigger for open-ended exploration — that belongs to the ideate skill.

**ideate**
- Do NOT trigger for "build this", "implement", or "write code" — those belong to the design or scaffold skills.
- Do NOT trigger for "explore the codebase", "where is X", "what calls Y" — exploring EXISTING code is the navigator skill; this skill explores SOLUTION APPROACHES and takes no codebase input.
- Do NOT trigger when the user hands over a markdown file and asks what it is missing — that is spec-interview, which sharpens an existing document rather than choosing between approaches.

**navigator**
- Do NOT trigger for action requests like "refactor X" or "add Y" — those go to action skills.
- Do NOT trigger for "explore my options", "what approaches could I take", or open-ended solution brainstorming — that is the ideate skill; this skill answers questions about code that already exists and never proposes an approach.

**orch**
- Do NOT trigger before a file-level decomposition exists; deciding what the pieces are belongs to architect and design, and orch runs after them, never instead of them.
- Do NOT trigger for work that fits in one inline edit, for anything inherently sequential, or for a repo whose own test command is unknown.
- Do NOT trigger for in-session Claude subagents sharing one working tree — that is superpowers:dispatching-parallel-agents; orch means separate git worktrees, a test gate before every merge, and non-Claude drivers.

**plan-tracker**
- Do NOT trigger for single-step tasks, quick questions, or exploratory ideation that hasn't committed to an approach.

**refactor**
- Do NOT trigger for new designs from scratch (use the design skill) or for bug fixes that don't involve structural changes.
- Do NOT trigger for READ-ONLY diagnosis — "is this well-structured", "review this code", "is this too complex", "audit the structure" ask for a report, not a change: use review-architecture (system structure), review-depth (module interfaces), or code-review (individual files).
- Do NOT trigger for prioritising or sequencing existing findings into a roadmap — that is refactoring-plan, whose Handoff this skill executes.
- Do NOT trigger for generating NEW files from a pattern — "new module", "add another X like Y", "stub this out" — that is scaffold; this skill only restructures code that already exists.

**refactoring-plan**
- Do NOT trigger for executing refactoring (use refactor), conducting reviews (use code-review or review-architecture), or designing new systems (use architect + design).

**review-architecture**
- Do NOT trigger for file-level code review (use code-review), refactoring execution (use refactor), or new system design (use architect + design).
- Do NOT trigger for module interface depth, cognitive load, information hiding, or "this is hard to navigate" — that is review-depth.
- Do NOT trigger for conflicts between instruction files, skills, or rule sets — that is compat-audit.

**review-depth**
- Do NOT trigger for general code review (use code-review), architecture scoring (use review-architecture), or refactoring execution (use refactor).
- Do NOT trigger for conflicts between instruction files, skills, or rule sets — that is compat-audit, which reads instruction files, never source code.

**scaffold**
- Do NOT trigger for refactoring existing code (use the refactor skill) or for open-ended design (use the design skill).

**spec-interview**
- Do NOT trigger for ideate — ideate takes a freeform problem with no input file and outputs a DECISION about which approach to take; spec-interview assumes the approach is chosen and outputs a BETTER DOCUMENT of that same approach.
- Do NOT trigger for design — design presumes requirements are known and produces dependency graphs, interfaces, and file structure; spec-interview produces prose, not structure, and never names a module or writes a Protocol.
- Do NOT trigger for grill-me — grill-me is unbounded conversational interrogation aimed at shared human/agent understanding and writes no file; spec-interview is bounded to 3 rounds and its deliverable is a file on disk.
- Do NOT trigger with no input file — without a markdown path this skill has nothing to interview.

## Workflow grouping

**Build chain** — `ideate → architect → design → scaffold`

- on disk: `ideate`, `architect`, `design`, `scaffold`

**Review chain** — `code-review + review-architecture + review-depth → refactoring-plan → refactor`

- on disk: `code-review`, `review-architecture`, `review-depth`, `refactoring-plan`, `refactor`

**Meta / cross-cutting** — `active in any workflow`

- on disk: `plan-tracker`, `navigator`, `orch`

**Ungrouped** — skills on disk not in any chain above: `compat-audit`, `spec-interview`

## Commands

`commands/*.md` are slash-command prompts, not skills. 1 of 16 declare YAML frontmatter; for the rest the purpose below is the file's first line, which is all that can be derived mechanically.

| Command | Frontmatter | Purpose (first line) |
|---|---|---|
| `/build` | no | Build a new Python package from scratch. |
| `/deep-clean` | no | Full deep clean: slim + navigate + review cycle. |
| `/execute-plan` | no | Execute the plan. |
| `/explain` | no | Explain this codebase for someone new. |
| `/extend` | no | I want to extend this codebase. |
| `/fix-pr` | no | Resolve PR review comments thoroughly. |
| `/full-build` | no | Full architecture-first build with iteration loops. |
| `/how` | no | Open the blueprint explainer page in a browser. |
| `/map` | no | Print a quick map of this blueprint installation. |
| `/navigate` | no | Make this codebase easier to discover and navigate. |
| `/research-triage` | no | This is a research codebase. |
| `/review-cycle` | no | Run a review-refactor-review cycle. |
| `/scout` | yes | Review an external GitHub repo and identify skills, agents, or patterns to incorporate into our AgentDS framework. |
| `/slim` | no | Reduce bloat in this codebase. |
| `/test` | no | Generate tests for this code. |
| `/triage` | no | Run a full triage on this codebase |

## Health

Mechanically checkable invariants from `CLAUDE.md`. Judgement-based invariants (diagnostic-vs-action separation, no duplicated principles) are not checked here.

No violations.

### One-sided negative triggers

`A` disclaims `B` by name in a `Do NOT trigger` sentence, but `B`'s description never names `A` at all. Negative triggers are written looking backward — the new skill disclaims the old, the old never learns about the new — so the collision is only blocked from one side. Fix by adding the reciprocal negative trigger to `B`.

Only checked between skills that produce the SAME output kind. Pointing at a skill that does a different job is routing, not a turf dispute, and demanding reciprocity for it would bloat every description with disclaimers of skills it never collides with.

None — every negative trigger between same-kind peers is reciprocated.

**Coverage** — every skill declares an `output:` kind; none is skipped.

### Drift in hand-maintained registries

Reported only — this tool never edits `README.md` or `shared-principles.md`.

- none

